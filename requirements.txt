# Dream-AI — dependências

# Núcleo: arquitetura do zero + verificador do sonho (rodam em qualquer máquina)
torch>=2.1.0
numpy>=1.24.0
tokenizers>=0.15.0

# Modelo Coder de 1B + sonho/consolidação (Colab Pro com GPU)
transformers>=4.40.0
datasets>=2.18.0
peft>=0.10.0          # LoRA / QLoRA
bitsandbytes>=0.43.0  # quantização 4-bit
accelerate>=0.29.0
